version: '3.9'

services:
  app:
    build: .
    ports:
      - "3002:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - APP_ENV=prod
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
